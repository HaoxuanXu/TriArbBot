package config

const (
	// account type (live or paper)
	LIVE_ACCOUNT  = "live"
	PAPER_ACCOUNT = "paper"

	// server type (staging or production)
	STAGING_SERVER    = "staging"
	PRODUCTION_SERVER = "production"
)
